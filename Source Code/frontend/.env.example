# Backend API URL (must match the backend PORT in backend/.env)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080/

# Socket.io URL (usually same as backend URL)
NEXT_PUBLIC_SOCKET_URL=http://localhost:8080/
