import React from 'react';
import MedicalChat from '../../../(site)/(pages)/(user)/user/message/page';

const page = () => {
    return (
        <div>
            <MedicalChat/>
        </div>
    );
};

export default page;